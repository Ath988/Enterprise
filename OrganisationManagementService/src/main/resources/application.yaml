server:
  port: 8087

spring:
  application:
    name: organisation-management-service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://192.168.1.106:5432/enterpriseorgdb
    username: postgres
    password: root
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update