server:
  port: 8090

spring:
  security:
    user:
      name: super_admin
      password: root
  application:
    name: TicketService
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: create-drop